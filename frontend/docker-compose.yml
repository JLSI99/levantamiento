services:
  frontend:
    build:
      context: .
      dockerfile: dockerfile
    container_name: ecosistema-frontend
    ports:
      - "8080:80"
    volumes:
      - ./src:/usr/share/nginx/html/src
    networks:
      - backend_net
    restart: always

networks:
  backend_net:
    external: true